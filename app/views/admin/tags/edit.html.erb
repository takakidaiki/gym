<%= form_with model: @tag,url: admin_tags_path do |tag| %>
  <h4>tag</h4>
  <%= tag.text_field :name %>
  <%= tag.submit '投稿' %>
  <%= link_to '一覧', admin_tags_path(tag) %>
  <%= link_to '削除', admin_tag_path(tag), method: :delete %>
<% end %>